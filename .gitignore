.next
node_modules
coverage
# Ignora TODA variante de .env (.env.production, .env.local, .env.*.local, ...).
# Antes eram só ".env" e ".env.local" literais, entao um .env.production seria commitado.
.env*
!.env.example
*.log
*.tsbuildinfo
.scannerwork
# Evita arquivos acidentais de chave SSH abertos pelo editor na raiz do projeto
*sshgithub_lob*
